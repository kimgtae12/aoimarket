<!DOCTYPE html>
<html>

<head>
    <%- include('include/header') %>

        <script src="../public/javascript/joinScript.js" type="text/javascript"></script>
        <link rel="stylesheet" href="../../public/stylesheet/subStyle.css">
        <script>
            let referrer = document.referrer;
            let splitRef = referrer.split("/");
            let lastRef = splitRef[splitRef.length - 1];
            console.log(lastRef);

            if (lastRef != "join") {
                alert("인증이 완료되지 않은 계정입니다.");
            }
        </script>
</head>

<body>
    <div class="wrapper">
        <%- include('include/toparea') %>
            <article class="middle_form">
                <h1 id="login_title">회원가입</h1>
                <hr id="horizon_line">

                <section class="join_form">
                    <div class="email_intro">
                        <p>
                            <%= session_id %>님의 회원가입을 환영합니다.
                        </p>
                    </div>
                    <form method="post" action="/emailCheck/verify">
                        <div class="form_line">
                            <p>인증번호 입력</p>
                            <div>
                                <div class="input_form">
                                    <input type="text" name="verify" required>
                                </div>
                            </div>
                        </div>
                        <input type="button" id="home_button" class="join_button cancle" value="취소">
                        <input type="submit" class="join_button access" value="회원가입">
                    </form>
                </section>
            </article>
    </div>

</body>

</html>